function logIt(message: string) {
    console.log(message);
}

let myMessage = 'Hello world!';
logIt(myMessage);

/**
 * 2 STeps to run this code
 * 
 * 1. Call the TypeScript compiler to TRANSPILE our TS -> to JS: tsc main.ts
 * 2. Run the JS file: node main.js
 */